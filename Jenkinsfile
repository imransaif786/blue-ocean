pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'ping localhost'
      }
    }

    stage('test') {
      steps {
        sh 'ping localhost'
      }
    }

    stage('deploy') {
      steps {
        sh 'ping localhost'
      }
    }

  }
}